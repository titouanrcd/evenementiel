# ============================================================
# FICHIER .HTACCESS - Redirection vers public/
# ============================================================
# SECURITE: Bloquer l'accès direct au dossier racine
# Tout le trafic est redirigé vers le dossier public/
# ============================================================

# Désactiver le listing des répertoires
Options -Indexes

RewriteEngine On

# Rediriger TOUTES les requêtes vers public/
# Y compris la racine du dossier
RewriteRule ^$ public/ [L]
RewriteCond %{REQUEST_URI} !^/evenementiel/public
RewriteRule ^(.*)$ public/$1 [L]
